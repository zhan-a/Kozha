# Database health — unknown HamNoSys tags in `data/*.sigml`

Generated by `scripts/scan_unknown_hns_tags.py`. Any tag listed below is a
name CWASA's shipped `allcsa.js` (HNSDefs.tokenNameMap) does not recognise.
When the avatar processes such a sign, the tag is silently dropped — and
the remaining HamNoSys sequence may fail tree-parsing, which surfaces as
`mismatched input '[object Object]'`.

Prompt 3 ships a runtime validator that skips affected signs and surfaces a
clear error instead of emitting broken XML. Fixing the underlying data is
owned by prompt 7.

## Totals

| tag | occurrences | likely fix |
|---|---|---|
| `<hampalmud/>` | 1682 | split into `<hampalmu/>` + `<hampalmd/>`, or use `<hamreplace/>` + `<hampalmd/>` if a rotation is intended |
| `<hammoveudl/>` | 127 | split into valid motion tags, e.g. `<hammoveul/>` or `<hammoved/>` + `<hammovel/>` |
| `<hampinchopen/>` | 38 | rename to `<hampinch12open/>` (or `<hamceeopen/>` if a cee shape is intended) |
| `<hamupperarm/>` | 35 | rename to `<hamUpperarm/>` (capital U — CWASA tag is case-sensitive) |
| `<hamindxfinger/>` | 2 | typo — should be `<hamindexfinger/>` |
| `<hamfinger234/>` | 1 | not a valid handshape — CWASA has `<hamfinger23/>` (2+3) and `<hamfinger2345/>` (2+3+4+5) |

## Per-file breakdown

### `data/Algerian_SL.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamupperarm/>` | 1 | 3385 |

### `data/French_SL_LSF.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hampalmud/>` | 185 | 9, 52, 57, 83, 88 (+180 more) |
| `<hammoveudl/>` | 10 | 773, 862, 1246, 1618, 4640 (+5 more) |
| `<hamupperarm/>` | 1 | 7071 |

### `data/German_SL_DGS.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamupperarm/>` | 9 | 1125, 2483, 6507, 14830, 14849 (+4 more) |

### `data/Greek_SL_GSL.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hampalmud/>` | 432 | 74, 883, 890, 1020, 1024 (+427 more) |
| `<hammoveudl/>` | 36 | 1441, 1725, 1950, 3794, 3909 (+31 more) |
| `<hamupperarm/>` | 3 | 5313, 16835, 17421 |

### `data/Indian_SL.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hampinchopen/>` | 38 | 1210, 1354, 1369, 1413, 1659 (+33 more) |

### `data/Kurdish_SL.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamupperarm/>` | 1 | 2433 |

### `data/Polish_SL_PJM.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hampalmud/>` | 1065 | 95, 121, 135, 152, 203 (+1060 more) |
| `<hammoveudl/>` | 81 | 98, 1788, 2553, 2811, 3709 (+76 more) |
| `<hamupperarm/>` | 18 | 2618, 2997, 3460, 5587, 6356 (+13 more) |

### `data/Vietnamese_SL.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamindxfinger/>` | 2 | 14993, 28317 |

### `data/asl_alphabet_sigml.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamfinger234/>` | 1 | 225 |

### `data/hamnosys_bsl_version1.sigml`

| tag | count | first occurrences (line numbers) |
|---|---|---|
| `<hamupperarm/>` | 2 | 2469, 11277 |

